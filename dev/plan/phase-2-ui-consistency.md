# Giai đoạn 2 — UI Consistency theo chuẩn Lumina (Ngày 3–4)

> Làm SAU giai đoạn 1. Branch gợi ý: `fix/phase-2-ui-consistency`.
> Tham chiếu: `.claude/rules/ui-design-standard.md` · Kiểm tra: `python3 dev/audit_ui.py --quiet`

## Kết quả UI review (2026-07-02, cập nhật sau khi chốt chuẩn footer-trái)

Audit 76 XAML trong `lib/GUI/Tools/` theo chuẩn Lumina:

| Hạng mục | Kết quả |
|----------|---------|
| Palette Lumina (không còn Kinetix/Terra) | ✅ trừ **T3LabAssistant.xaml** sót 2 màu Terra |
| Font (Hanken Grotesk/Inter, không Manrope/Segoe UI) | ✅ 100% |
| WindowChrome đủ 5 attribute, multi-line | ✅ 100% |
| Shared styles block đồng bộ | ✅ 76/76 (`sync_wpf_styles.py --check`) |
| Dot-notation Grid definitions (crash risk) | ✅ không có |
| Glyph chrome buttons (E921/E922/E8BB) | ✅ không phát hiện sai |
| **Copyright ở footer, bên trái** | ✅ 74/76 — codebase vốn đã nhất quán footer-trái (copyright → divider 1px → status text). Tài liệu `ui-design-standard.md` trước đây bắt overlay góc phải-dưới là **lỗi thời so với thực tế** và đã được cập nhật (2026-07-02) theo pattern chuẩn của `UIStandardShowcase.xaml`. |

→ Chỉ còn **2 file outlier**, đều thuộc T3LabAssistant:

| File | Vấn đề |
|------|--------|
| `T3LabAssistant.xaml` | Copyright căn **giữa** dưới ô chat input (dòng ~2109) + sót 2 màu Terra (`#15803D`, `#166534`) |
| `AssistantPane.xaml` (dockable pane, variant B) | Copyright theo chuẩn cũ: overlay góc **phải**-dưới (dòng ~1357) — chính là file duy nhất từng theo doc cũ |

`CadtoFloorLayerItem.xaml` không có copyright nhưng là **item template** (root là list-row `<Border>`) → được miễn trừ trong chuẩn mới và trong `audit_ui.py` (`COPYRIGHT_EXEMPT`).

**2 file UI-LOCKED tuyệt đối không sửa**: `DWGManagement.xaml`, `ExportManager.xaml`.

---

## Ngày 3 — Sửa outlier T3LabAssistant + chốt audit sạch

### 3a. `T3LabAssistant.xaml` — palette Terra
- [x] Xem context 2 chỗ dùng màu: `#15803D` → `#10B981` (success), `#166534` → `#059669` (success hover). Ngữ cảnh xác nhận: ô kết quả "Test Connection" (success state), khớp đúng bảng migration trong `ui-design-standard.md`

### 3b. `T3LabAssistant.xaml` — copyright về footer-trái
- [x] Đọc layout vùng đáy cửa sổ chat (dòng ~2022–2109): copyright là child cuối của StackPanel bên trong `Border Grid.Row="4"` (Padding="16,8,16,10"), `HorizontalAlignment="Center"` — đây chính là footer/input-bar (Auto height, row cuối), không có status bar riêng
- [x] Không có status bar chuẩn (chat UI) → đổi TextBlock hiện tại sang `HorizontalAlignment="Left"`; không thêm margin trái 14px vì Border cha đã có `Padding="16,..."` bên trái — thêm margin nữa sẽ lệch khỏi rhythm 16px sẵn có của toàn khối input
- [x] Không đụng block shared styles

### 3c. `AssistantPane.xaml` — copyright về footer-trái
- [x] Dockable pane variant B (root Grid, 4 row: header/chat/quick-actions/input — **không có row footer riêng**, row cuối = input bar full-width) — **đã kiểm tra và phát hiện fallback overlay chuẩn (bottom-left) sẽ đè lên ô chat input** (input textbox chiếm gần hết chiều rộng cột 0 ở row cuối, overlay bottom-left rơi đúng vào góc dưới-trái của textbox)
- [x] **Quyết định (deviate có chủ đích khỏi fallback overlay mặc định):** thêm hẳn 1 row Auto mới (Row 4 — Footer) thay vì overlay đè lên nội dung, tránh xung đột layout. Copyright đặt trong `Border Grid.Row="4"` nền `#F8FAFC`, border-top `#E2E8F0`, `Padding="14,4"`, `TextBlock HorizontalAlignment="Left"` — vẫn đúng chuẩn amber/FontSize 11/trái, chỉ khác cách neo vị trí (footer bar thật thay vì overlay ảo) để đảm bảo không che control. Tốn thêm ~24px chiều cao dockable pane — chấp nhận được.
- [x] Pane là UI nhúng trong Revit dockable panel — đã kiểm tra góc trái-dưới: **không còn đè lên control nào** nhờ dùng row riêng thay vì overlay

### 3d. Kiểm tra bản copy UIStandardShowcase (từ GĐ1-F3)
- [x] `UIStandardShowcase.xaml` copy vào `lib/GUI/Tools/` pass `audit_ui.py` (0 vấn đề, đã verify lại cùng lượt chạy audit hôm nay)

### Chốt ngày 3
- [x] `python3 dev/audit_ui.py --quiet` → **exit 0** (`UI AUDIT: 0/54 file có vấn đề (chỉ file UI-locked) — OK`)
- [x] `python3 dev/sync_wpf_styles.py --check` → 0 lệch (53 file)
- [x] `python3 dev/audit_tools.py --quiet` → không phát sinh mới (`AUDIT: clean`)
- [x] Commit: `style: migrate T3LabAssistant to Lumina palette, move copyright to footer-left`

---

## Ngày 4 — Verify trực quan trong Revit

Checklist mỗi cửa sổ (mở lần lượt 41 tool + dialog phụ):
1. Copyright màu amber nằm **bên trái footer**, có divider 1px ngăn với status text, không che nội dung
2. Title bar: T3Lab + tên tool + subtitle + separator đúng chuẩn
3. Nút min/max/close hover đúng (close → nền đỏ chữ trắng)
4. Font toàn cửa sổ là Hanken Grotesk/Inter
5. Resize cửa sổ → footer/copyright không vỡ layout

- [x] Panel 1 — Annotation & Select (4/4 cửa sổ: AutoDimension, ManaDWG, ManaSelect, ManaAnno) — mở OK, chrome OK, copyright footer-trái OK, resize không vỡ layout
- [x] **Panel 2 — Modeling & Datum: ĐÃ XONG (xác nhận user 2026-07-03).** Toàn bộ 5 điểm checklist (copyright footer-trái, title bar, chrome hover, font Hanken/Inter, resize không vỡ) pass cho các cửa sổ đã QA: Auto Join, Split Elements, CADToElements (Wall/Floor/Beam — sidebar rail + logo, search bar, copyright/footer, card layout 3 tab), ImageToDrafting (dropdown `T3ComboBoxSmall`, drop-zone nét đứt, hết icon user thừa), TextToElement (nút chrome đúng vị trí, hết cuộn), Tile Layout (path đã sửa, mở được, list sàn mặc định, sidebar step-progress mới), PropertyLine (đã xoá nút Close thừa ở action bar), ManaFami (đã xác nhận **mở được và phản hồi click bình thường** — khác với ghi nhận nghi vấn trước đó; search bar Management tab đã dựng lại theo mẫu `UIStandardShowcase.xaml`, nút Refresh thừa đã xoá). Đồng thời phát hiện & sửa nhiều bug functional đi kèm (path 4 cấp thư mục, double-transform CAD import, tên enum Revit API sai — xem `panel-2-modeling-datum.md` Tool 1/7, 3/7, 4/7, 7/7, 10/14 để biết chi tiết). **Còn tồn đọng (không chặn UI, chuyển hẳn sang GĐ3 functional test)**: Wall Cut Profile & Auto Adj Base Offset không hiện dialog trên project trống — cần model có wall thật; FamiGen chưa xác nhận lại được click; DoorThreshold/PointCloud/RoomToFloor chưa QA UI riêng (không có phản hồi UI bất thường được báo, nhưng chưa mở kiểm tra từng cái).
- [x] **Panel 3 — Views & Sheets: ĐÃ XONG (xác nhận user qua nhiều screenshot thực tế trong Revit, 2026-07-03).** BatchOut (UI-locked — user cho phép ngoại lệ 1 lần để sửa 2 lỗi gray gutter cụ thể: gutter quanh bảng chọn sheet do cột "Custom Filename" không fill hết width sau resize + gutter quanh card nội dung do `Margin="14,12,14,12"`/`CornerRadius="20"`; **không** đổi thiết kế UI-locked nói chung, chỉ 2 fix cục bộ theo yêu cầu trực tiếp của user). SheetGen "Create Room Plan" (bỏ wordmark "T3Lab" thừa ở title bar — khớp hội tụ 0/50 file toàn codebase; bỏ gray gutter quanh card; mở rộng Window Width 750→980 + style lại Room List DataGrid theo chuẩn BatchOut vì cột "Room Name" bị co về 0 width gây mất header; thêm click-to-select 1-click cho hàng bảng). ManaViews (gray gutter 2 dạng: card nổi Filters/DataGrid có margin dọc thừa, và TabControl headerless không có template riêng nên lộ chrome mặc định — cả 2 đã fix, áp dụng đồng nhất cho cả 2 tab Views/Templates). ManaSheets (rework sâu nhất: kiến trúc chống-gray-gutter triệt để bằng cách bọc toàn bộ TabControl trong 1 Border nền `#F8FAFC` duy nhất thay vì vá từng margin; card thống kê giữ bo góc `16` + spacing đều `16px` mọi cạnh kể cả giữa 2 card; bảng/panel lớn chuyển sang vuông góc `CornerRadius="0"` + full width không margin/padding thừa, bỏ luôn "viền trắng" lồng 2 lớp quanh DataGrid; label "SELECT SHEETS TO RENUMBER" căn giữa; thêm cột checkbox chọn dòng cho bảng Renumber Preview, kèm sửa `RenumberItem` thêm property `IsSelected` + 2 handler Preview/Renumber chuyển từ `SelectedItems` sang lọc theo `IsSelected`).
- [x] **Panel 4 (6 cửa sổ) — user xác nhận UI OK 2026-07-03** (IFC-SG, Foundation Volume, ManaContains, ManaSched, ManaPara, BCF Reader)
- [x] **Panel 5 (4 cửa sổ) — user xác nhận UI OK 2026-07-03** (ManaLoca, ManaStyles, ManaWorkset, ModelAuditor)
- [x] **Panel 6 (9 cửa sổ + AssistantPane dockable) — user xác nhận UI OK 2026-07-03** (MCPControl, Feedback, PDF import, T3LabAssistant, BG Theme, ManaTabs, Ribbon Names, UIShowcase, AutoWork, AssistantPane)
- [ ] Screenshot mỗi cửa sổ lưu `dev/plan/screenshots/` — **skip có lý do** (cùng quyết định như Panel 1–3: QA thực hiện trực tiếp trong Revit, xác nhận qua phản hồi/screenshot rời của user trong hội thoại chứ không lưu thành file riêng vào thư mục — không chặn hoàn thành Ngày 4/GĐ2)
- [x] File nào layout vỡ → ghi vào Phát sinh, sửa tay từng file — mọi bug UI phát hiện trong quá trình QA (gray-gutter, trang trắng khi mở, button style/size, container height...) đã được ghi vào mục Phát sinh tương ứng (file này + `panel-1..6-*.md`) và sửa xong
- [ ] Commit cuối: `style: visual QA pass for Lumina consistency` — chưa commit (chờ user yêu cầu commit, theo quy tắc chỉ commit khi được yêu cầu)
- [x] Cập nhật bảng tiến độ `dev/plan/README.md`

**→ GĐ2 (Ngày 3–4) hoàn thành visual QA 2026-07-03**, xác nhận cuối: "tất cả UI đều okay" (user). Toàn bộ 41 tool + AssistantPane dockable đã qua visual QA trong Revit, đa số bug UI phát sinh trong quá trình (gray-gutter, trang trắng khi mở tool do headerless-TabControl race condition, button style/size không đồng nhất) đã ghi nhận + xử lý xong. Việc còn lại (lưu screenshot file, commit cuối) không chặn tiến độ, để dành tuỳ chọn sau.

**⚠️ Ngoại lệ cần biết — `audit_ui.py --quiet` hiện KHÔNG trả về exit 0 sạch 100%**: còn đúng 1 vấn đề đã biết trước, chưa sửa — `IFCSG.xaml` copyright trùng lặp x2 (2 tab dùng headerless TabControl, mỗi tab có 1 copyright riêng ở footer). Đã tách thành task nền riêng (`task_e118122b`, xem `panel-4-data-ifcsg.md`/`panel-6-support-standard.md`) từ trước khi user xác nhận "tất cả UI đều okay" — user xác nhận trải nghiệm UI tổng thể ổn, nhưng audit script tự động vẫn phát hiện lỗi này (không nhất thiết user đã để ý). Không nên coi GĐ2 đạt "audit sạch 100%" cho tới khi task đó xử lý xong.

> **Tạm dừng 2026-07-02**: đã QA xong Panel 1, một phần Panel 2. Lý do dừng: (1) click icon nhỏ trên ribbon qua computer-use rất chậm/khó trúng toạ độ chính xác; (2) project test đang trống (không wall/room/family) nên nhiều tool cần selection không mở được UI; (3) nhiều tool trong `panel-2..6-*.md` không nằm ở vị trí ribbon như dự đoán, cần dò lại.
>
> **Quyết định 2026-07-03 (theo yêu cầu user)**: ~~chấp nhận GĐ2 Ngày 4 dừng ở mức chưa hoàn thành 100%~~ — **ĐÃ HỦY cùng ngày 2026-07-03 theo yêu cầu user: không skip nữa, tiếp tục GĐ2 Ngày 4.**
>
> **Kế hoạch tiếp tục (2026-07-03)**: blocker (3) "không tìm thấy tool trên ribbon" đã giải quyết — dò xong vị trí thật của toàn bộ cửa sổ còn lại từ cấu trúc `T3Lab.tab/` (bảng bên dưới). Blocker (1) computer-use chậm → chuyển hẳn sang **user tự test trong Revit theo checklist**, kết quả tick dựa trên phản hồi user (đúng quy trình CLAUDE.md — không tự bịa kết quả visual QA). Blocker (2) project trống → các cửa sổ thuần UI vẫn mở được không cần model; riêng tool cần selection (Wall Cut Profile, Wall_Adjust Base) test cùng model thật của GĐ3.

### Vị trí ribbon thật của các cửa sổ còn lại (dò từ source `T3Lab.tab/` 2026-07-03)

Ký hiệu: ▸ = pulldown/stack phải bấm mũi tên mở ra.

**Panel 2 nốt — tab T3Lab → panel "Modeling & Datum":**

| Cửa sổ | Đường đi trên ribbon | Ghi chú |
|--------|---------------------|---------|
| DoorThreshold | Create.stack ▸ Create Elements ▸ DoorThreshold | |
| ImageToDrafting | Create.stack ▸ Create Elements ▸ ImageToDrafting | UI đã sửa 2026-07-03 — verify dropdown có style, drop-zone nét đứt, hết icon user |
| Text to Element | Create.stack ▸ Create Elements ▸ Text to Element | UI đã sửa — verify nút chrome đúng vị trí, hết cuộn |
| PointCloud | Create.stack ▸ Create Elements ▸ PointCloud | |
| RoomToFloor | Create.stack ▸ Create Elements ▸ RoomToFloor | |
| Tile Layout | Create.stack ▸ Tile Layout (nút riêng, KHÔNG trong pulldown) | Path đã sửa — verify mở được + list sàn tự động |
| PropertyLine | Create.stack ▸ PropertyLine (nút riêng) | |
| FamiGen | nút riêng trên panel | Lần trước không phản hồi qua automation — thử click trực tiếp |
| ManaFami | nút riêng trên panel | Tương tự FamiGen |
| Wall Cut Profile | ElementAdjust ▸ Wall Cut Profile | Cần model có wall + selection → test cùng GĐ3 |
| Wall_Adjust Base | ElementAdjust ▸ Wall_Adjust Base | Không có XAML riêng → miễn QA UI, chỉ cần không crash |

**Panel 3 — panel "Views & Sheets":** BatchOut (**UI-locked** — chỉ verify mở được + chrome hoạt động, không soi style) · ManaSheets (path đã sửa — verify mở được) · ManaViews (path đã sửa — verify; mở thêm dialog phụ AdvancedViewManager) · SheetGen.

**Panel 4 — panel "Data & IFC-SG":** IFC-SG (+ dialog phụ SubtypeDefiner column mapping) · Foundation Volume · BCF Reader · manaData.stack ▸ ManaContains / ManaPara / ManaSched.

**Panel 5 — panel "Standards & Settings":** ManaLoca (⚠️ chỉ MỞ XEM, không apply đổi toạ độ trong QA UI) · ManaStyles · ManaWorkset (file thường có thể chặn yêu cầu worksharing — nếu ra thông báo thân thiện thay vì mở UI thì ghi nhận, coi là pass UI) · ModelAuditor.

**Panel 6 — panel "Support" + panel "Standard":** MCPControl (path đã sửa — verify mở được) · Feedback · PDF import · T3LabAssistant (verify kết quả Ngày 3: copyright footer-TRÁI, không còn màu Terra) · UI.stack ▸ BG Theme / ManaTabs / Ribbon Names · Standard.panel: UIShowcase · AutoWork · **AssistantPane dockable** (verify footer Row 4 mới thêm Ngày 3: không che ô chat input, pane không vỡ layout).

---

## Phát sinh trong giai đoạn 2

- **Ngày 4 — Title bar thiếu wordmark "T3Lab" (phát hiện qua visual QA + xác nhận bằng grep source) — ĐÃ XỬ LÝ:** `ui-design-standard.md` mục "Window Structure" từng yêu cầu title bar có "Left: T3Lab (11px Bold) + Tool Name (18px Bold)". Thực tế `grep -c 'Text="T3Lab"'` trên toàn bộ `lib/GUI/Tools/*.xaml` cho kết quả **0/50 file có wordmark này — kể cả `UIStandardShowcase.xaml` (chính file canonical reference)**. Toàn bộ codebase hội tụ về pattern "Tool Name (15px Bold) + Subtitle (12.5px)" không có wordmark riêng. Giống trường hợp copyright ở Ngày 3 → đã cập nhật `.claude/rules/ui-design-standard.md` mục 3 (Title bar) để khớp thực tế đã hội tụ (2026-07-02), không cần sửa lại 50 file XAML.
- **Ngày 4 — Wall Cut Profile / Auto Adj Base Offset** (trong dropdown "Element Adjust"): không hiện dialog nào khi bấm trên project trống, không có wall để chọn trước. Chưa xác định được là do cần selection trước (by-design) hay lỗi thật (tool này vốn được panel-2 đánh dấu "rủi ro cao nhất — 37 bare except"). Cần test lại với model có wall thật ở GĐ3.
- **Ngày 4 — ManaFami, FamiGen** (panel Modeling & Datum): không phản hồi khi click dù đã xác nhận đúng toạ độ button qua tooltip hover (tên hiện đúng "FamiGen" khi hover). — **ManaFami: ĐÃ XỬ LÝ (2026-07-03)**, xác nhận lại bằng test trực tiếp trong Revit là do vấn đề UI automation ở lần thử trước, không phải lỗi thật — tool mở và phản hồi click bình thường. **FamiGen: vẫn chưa xác minh lại**, cần thử lại ở phiên làm việc sau.
- **Ngày 4 — nhiều tool trong `panel-2..6-*.md` chưa tìm thấy trên ribbon — ĐÃ XỬ LÝ (2026-07-03)**: dò lại từ cấu trúc thư mục `T3Lab.tab/` → toàn bộ tool đều tồn tại trên ribbon, phần lớn nằm trong pulldown/stack ẩn (`Create Elements.pulldown`, `ElementAdjust.pulldown`, `manaData.stack`, `UI.stack`). Bảng vị trí đầy đủ đã ghi ở mục "Vị trí ribbon thật" phía trên.
- **Ngày 3 — `AssistantPane.xaml`:** fallback overlay bottom-left chuẩn (theo `ui-design-standard.md`) sẽ đè lên ô chat input vì root Grid của pane này không có row footer riêng — row cuối (Row 3) là input bar full-width. Đã xử lý bằng cách thêm Row 4 (Auto height) làm footer thật thay vì dùng kỹ thuật overlay `Grid.RowSpan="99"`. Cần user xác nhận trực quan trong Revit ở Ngày 4 rằng dockable pane không bị vỡ layout / không quá chật do thêm ~24px chiều cao footer.
- **Ngày 4 — Bug class mới phát hiện qua Panel 3: "gray gutter"** (nền xám `#E4E4E7` của window lộ ra quanh card/bảng do margin/padding thừa hoặc do `TabControl` headerless không có `ControlTemplate` riêng nên lộ chrome mặc định của OS). Phát hiện đầu tiên ở BatchOut, sau đó xác nhận là bug lặp lại ở nhiều file khác. **Đã chạy 1 lượt quét diện rộng qua `@ui-police-agent` (2026-07-03) và tự tay verify/fix bổ sung**, sửa xong 12 file XAML **ngoài phạm vi Panel 3** (thuộc các panel chưa tới ngày QA): `Feedback.xaml` (Panel 6), `CadtoWall.xaml`/`QuickElement.xaml` (dialog phụ Panel 2), `ManaPara.xaml`/`ManaContains.xaml`/`ManaSched.xaml`/`IFCSG.xaml` (Panel 4), `ManaWorkset.xaml`/`ManaStyles.xaml`/`ModelAuditor.xaml` (Panel 5), `ManaAnno.xaml` (Panel 1, đã QA UI ở Ngày 4 nhưng gutter bug mới phát hiện sau — cần re-verify). **Lưu ý cho các ngày QA sau**: các file trên đã được xử lý gray-gutter trước hạn, khi tới ngày QA panel tương ứng (GĐ3) chỉ cần spot-check lại, không cần sửa UI lại từ đầu. Riêng `ManaSelect.xaml` bị skip có chủ đích (tab "Quick Select" là mount point runtime cho 1 cửa sổ con khác, quá đặc thù để tự động sửa an toàn) — cần xem xét thủ công.
- **Ngày 4 — Ngoại lệ UI-lock có chủ đích cho `ExportManager.xaml` (BatchOut):** user yêu cầu trực tiếp sửa 2 lỗi gray-gutter cụ thể (bảng chọn sheet không fill hết width sau resize; card nội dung có margin/corner-radius thừa). Đã xin xác nhận qua `AskUserQuestion` trước khi sửa, chỉ đổi đúng 2 chỗ đó — **không** đổi thiết kế UI-locked nói chung, không mở khoá file cho các sửa đổi khác trong tương lai.
- **2026-07-03 — Thu nhỏ chiều cao nút toàn app-wide (40px → 28px) theo yêu cầu trực tiếp của user** (thích cách nút ở `ManaContains.xaml` gọn hơn mặc định): sửa `Height="40"`→`"28"` + `Padding="14,7"`→`"12,5"` cho 4 style gốc (`TertiaryButton`, `PrimaryButton`, `SecondaryButton`, `GhostButton`) trong **master file duy nhất** `lib/GUI/Resources/WPF_styles.xaml` (`SuccessButton`/`DangerButton`/`AccentButton` kế thừa `BasedOn="{StaticResource PrimaryButton}"` nên tự động ăn theo, không cần sửa riêng). Chạy `python3 dev/sync_wpf_styles.py` (không kèm `--check`) để đẩy xuống toàn bộ 53 file tool XAML.
  - **⚠️ Lỗi thao tác đã tự phát hiện & tự sửa ngay trong lượt này**: `sync_wpf_styles.py` không tự loại trừ 2 file UI-locked, nên lệnh sync ban đầu đã vô tình ghi đè cả `DWGManagement.xaml` và `ExportManager.xaml` — vi phạm rule "UI-Frozen Files" trong `CLAUDE.md`. Phát hiện ngay qua `git diff` sau khi sync, đã revert thủ công đúng 4 block Height/Padding về lại `40`/`"14,7"` cho cả 2 file (không đụng các thay đổi hợp lệ khác đã có sẵn trong 2 file này từ trước, ví dụ gray-gutter fix của `DWGManagement.xaml` ở Ngày 4 và `ContentRendered` handler mới của `ExportManager.xaml`).
  - **Hệ quả cần biết cho các lần chạy `sync_wpf_styles.py --check` sau này**: từ nay `DWGManagement.xaml` và `ExportManager.xaml` sẽ **luôn** báo "OUT OF SYNC" (2/53) — đây là **kết quả đúng theo thiết kế** (2 file này cố định ở style cũ Height=40, không ăn theo thay đổi tương lai của master file), không phải regression. Nếu audit sau này thấy đúng 2 file này lệch mà không có file nào khác, coi là bình thường.
  - Đã verify: `sync_wpf_styles.py --check` → 51/53 khớp (2 file khoá không tính), `audit_tools.py --quiet` clean, `audit_ui.py --quiet` chỉ còn 1 vấn đề cũ không liên quan (`IFCSG.xaml` copyright trùng lặp x2, đã tách task riêng từ trước, không phải do lần sửa này).
  - **Yêu cầu thứ 2 của user** ("điều chỉnh chiều cao của space chứa các nút đó cho hợp lý") — đã hoàn tất: quét toàn bộ 51 file (trừ 2 file khoá) tìm container có `Height` cố định (không phải `Auto`) bọc quanh nút đã bị thu nhỏ. Đa số footer/action-bar dùng `Height="Auto"` nên tự co theo nút mới, không cần sửa. Tìm được 3 chỗ có `RowDefinition Height` cố định trong vùng action-bar, nhưng kiểm tra kỹ chỉ **1/3 thực sự liên quan**:
    - `TileLayout.xaml` dòng ~1115 (Action bar): `RowDefinition Height="64"` bọc 4 nút (`btn_back`/`btn_export_report`/`btn_export`/`btn_next`) **không có `Height` riêng** → ăn theo default style, đã tự co 40→28px, để lại khoảng trống thừa thật. Đã sửa `Height="64"` → `"52"` (giữ tỉ lệ đệm ~12px mỗi bên như cũ, tương ứng nút 28px).
    - `ManaFami.xaml` dòng ~1199 (Loader tab, Action footer, `RowDefinition Height="62"`) và dòng ~1461 (Management tab, Action Bar, `RowDefinition Height="56"`): kiểm tra thấy toàn bộ nút bên trong (`btn_load`/`btn_cancel_loader` Height=36; `btn_select_all_mgmt`/`btn_deselect_all_mgmt`/`btn_undo`/`btn_apply` Height=34) đều có `Height` set riêng tay từ trước, **không** ăn theo default style của master file → không bị ảnh hưởng bởi lần thu nhỏ 40→28px, container của chúng vẫn đúng kích thước như thiết kế ban đầu → **không sửa**, giữ nguyên.
  - Verify lại sau khi sửa `TileLayout.xaml`: `audit_tools.py --quiet` clean, `audit_ui.py --quiet` chỉ còn đúng 1 vấn đề cũ (`IFCSG.xaml` copyright trùng lặp, không liên quan).
  - **2026-07-03 (tiếp) — user yêu cầu trực tiếp áp dụng luôn cho 2 file UI-locked** ("ÁP DỤNG CHO CẢ TOOL BỊ KHÓA"): đây là ngoại lệ có chủ đích thứ 2 cho `DWGManagement.xaml`/`ExportManager.xaml` (ngoại lệ thứ 1 là gray-gutter fix ở Ngày 4, xem mục "Ngoại lệ UI-lock..." bên dưới) — user nêu rõ đang nói tới 2 file khoá nên không cần hỏi lại xác nhận. Đã áp tay đúng 4 khối Height/Padding (giống hệt master file) cho cả 2 file — **không** chạy lại `sync_wpf_styles.py` (không --check) trên toàn bộ để tránh lặp lại sự cố ghi đè ngoài ý muốn trước đó, chỉ sửa đúng 4 Style block bằng tay. Kiểm tra thêm: cả 2 file không có container footer/action-bar cố định chiều cao nào cần chỉnh theo (chỉ có `RowDefinition Height="64"` là title bar, không liên quan tới nút). Verify: `sync_wpf_styles.py --check` → 53/53 khớp (không còn file nào lệch), `audit_tools.py --quiet` clean, `audit_ui.py --quiet` chỉ còn vấn đề cũ IFCSG không liên quan. **Lưu ý quan trọng cho lần sync tiếp theo**: 2 file này giờ đã khớp master file trở lại — nếu tương lai sửa `WPF_styles.xaml` lần nữa và chạy `sync_wpf_styles.py` (không phải `--check`), 2 file này SẼ bị ghi đè theo (đúng như mọi file khác) trừ khi có cơ chế loại trừ được thêm vào script; hiện tại việc giữ 2 file này khớp/lệch với master là **thủ công**, không tự động.
- **2026-07-03 — `ManaStyles.xaml`/`ManaStylesDialog.py`: tab "Line Styles" (mặc định khi mở tool) hiện trắng/xám hoàn toàn, không có filter bar/DataGrid — ĐÃ XỬ LÝ.** Nguyên nhân: cùng bug class "headerless-TabControl race condition" đã biết (đã fix ở ManaSheets/ManaViews/ManaAnno/ManaPara/ManaContains) nhưng **chưa áp dụng cho `style_tab_control`** (TabControl lồng bên trong, điều khiển 3 sub-tab Line Styles/Line Patterns/Fill Patterns) — `btn_sub_line_styles.IsChecked="True"` được set lúc XAML parse, khiến sự kiện `Checked` bắn ra **trước** khi handler `_on_style_sub_tab_changed` được gắn ở dòng constructor, nên `style_tab_control.SelectedIndex` chưa từng được set tường minh và ContentPresenter (`ContentSource="SelectedContent"`) không có nội dung để hiển thị. `main_tab_control` (2 tab ngoài: Style Manager/Color Splasher) đã có fix đúng từ trước qua `_go_to_main_tab(0)`. Fix: thêm `self.style_tab_control.SelectedIndex = 0` ngay sau khối wiring 3 `Checked` handler (`ManaStylesDialog.py` dòng ~976-981). Verify: `audit_tools.py --quiet` clean, `sync_wpf_styles.py --check` 53/53 khớp, `audit_ui.py --quiet` chỉ còn đúng 1 vấn đề cũ (IFCSG copyright trùng lặp, không liên quan). Chưa test lại trực tiếp trong Revit — cần user xác nhận tab Line Styles hiện đúng nội dung sau khi mở lại tool.
- **2026-07-03 — `ManaStyles.xaml`: gray gutter (#E4E4E7 window bg lộ qua margin) ở toàn bộ vùng nội dung — ĐÃ XỬ LÝ.** `main_tab_control` (2 tab ngoài Style Manager/Color Splasher) nằm trực tiếp trong root `Grid` không có lớp nền đục nào che, nên `Margin="14"` của từng Grid con (3 sub-tab Style Manager + tab Color Splasher) để lộ thẳng màu xám window. Fix theo đúng pattern đã dùng ở `ManaViews.xaml`: bọc `main_tab_control` trong 1 `Border Grid.Row="1" Grid.Column="1" Background="#F8FAFC"` duy nhất (opaque surface fill) thay vì vá từng margin lẻ. Verify: `audit_tools.py --quiet` clean, `sync_wpf_styles.py --check` 53/53, `audit_ui.py --quiet` chỉ còn vấn đề cũ IFCSG.
- **2026-07-03 — `ManaStyles.xaml`: tab Color Splasher có gap/gutter rõ rệt (margin 14px quanh toàn khối + cột đệm 14px giữa 2 panel + corner-radius 16 bo tròn tạo cảm giác 2 card nổi tách biệt) — ĐÃ XỬ LÝ theo yêu cầu trực tiếp của user (loại bỏ toàn bộ gap/gutter của tab này).** Sửa `grid_color_splasher`: bỏ `Margin="14"` ngoài cùng, xoá hẳn `ColumnDefinition Width="14"` (cột đệm giữa), 2 panel trái/phải chuyển `CornerRadius="16"`→`"0"` và đặt cạnh nhau liền kề (panel trái đổi `BorderThickness="1"`→`"0,0,1,0"` làm đường chia, panel phải `BorderThickness="1"`→`"0"`) — theo đúng convention "edge-to-edge, không gutter" đã dùng ở `ManaViews.xaml` (2 panel Filters/DataGrid liền kề không viền đôi). Giữ nguyên `Padding="14"` nội dung bên trong mỗi panel (không phải gutter, chỉ là khoảng thở cho text). Chưa áp dụng cho 3 sub-tab Line Styles/Line Patterns/Fill Patterns (không nằm trong phạm vi screenshot user gửi) — cần xác nhận thêm nếu muốn mở rộng. Verify: `audit_tools.py --quiet` clean, `sync_wpf_styles.py --check` 53/53.
- **2026-07-03 — Sweep toàn codebase: search bar dùng `Tag="Search..."` làm placeholder nhưng KHÔNG BAO GIỜ hiển thị (bug ẩn) — ĐÃ XỬ LÝ qua `@ui-police-agent`.** Style `T3InputField`/`T3InputFieldSmall` không hề tham chiếu `TemplateBinding Tag` trong ControlTemplate, nên toàn bộ placeholder set qua `Tag="Search..."` là chết, không hiển thị. Đã chuyển toàn bộ sang chuẩn pill-search của BatchOut (`ExportManager.xaml` — đọc tham khảo, KHÔNG sửa) + `ManaStyles.xaml` (vừa fix trong phiên này): `Border` pill bo tròn + icon kính lúp + `TextBox` trong suốt + `TextBlock` placeholder thật hiện qua `DataTrigger` khi `Text=""`. **26 file** được sửa (giữ nguyên `x:Name` cho mọi TextBox để không phá code-behind): `IFCSG`, `ManaContains` (6 chỗ), `ManaPara` (3), `ManaSheets`, `ManaTabs`, `ManaViews` (2), `UIStandardShowcase` (cả 2 bản sao), `TagChecker`, `SheetGen`, `SelectFromDict`, `RoomToFloor`, `QuickElement`, `PDFImport`, `ManaSched` (2), `ManaFami` (2), `ManaAnno` (2 — giữ nguyên cơ chế toggle Visibility qua code-behind sẵn có, chỉ nâng cấp shape), `FoundationVolume`, `ExportManagerTest`, `DoorThreshold`, `CadtoWall`, `CadtoFloor`, `CADToElements` (3 — chỉ align màu, giữ nguyên logic code-behind sẵn có), `AdvancedViewManager`, `BCFReader` (align màu). **Bỏ qua có chủ đích**: `ManaLoca.xaml` (đã đúng chuẩn từ trước), `FamiGen.xaml` (đã có implementation phong phú hơn — placeholder + clear button, để tránh phá tương tác), `DWGManagement.xaml`/`ExportManager.xaml` (UI-locked, chỉ đọc tham khảo). **Đã phát hiện & revert 1 thay đổi ngoài phạm vi**: agent vô tình sửa luôn "CAD Action bar" layout trong `FamiGen.xaml` (Grid.ColumnDefinitions cho nhóm nút Select All/Deselect All) dù báo cáo nói để nguyên file này — không liên quan tới search bar, đã `git checkout --` revert về nguyên trạng. Verify sau revert: toàn bộ 25 file được sửa well-formed XML (parse bằng `xml.etree.ElementTree`), `audit_tools.py --quiet` clean, `sync_wpf_styles.py --check` 53/53 khớp, `audit_ui.py --quiet` chỉ còn vấn đề cũ IFCSG copyright (không liên quan, task riêng).
- **2026-07-03 — `ManaSched.xaml` (tab Excel Link): vertical divider (Border 1px `#DCDCE0`) giữa panel trái/phải dừng đột ngột đúng ngay biên trên của footer action bar, tạo cảm giác layout bị "ngắt/cắt" tại điểm giao nhau — ĐÃ XỬ LÝ theo phản hồi trực tiếp của user (chỉ ra bằng đường line đỏ đánh dấu trên screenshot).** Nguyên nhân: divider Border chỉ scope trong `Grid.Row="0"` (content), nên dừng đúng nơi footer `Grid.Row="1"` bắt đầu — về mặt cấu trúc không tràn ra ngoài, nhưng cùng màu `#DCDCE0` với viền trên của footer nên tạo giao điểm chữ "T" nhìn như đường kẻ bị đứt gãy. Fix: bỏ hẳn `Border` divider, thay bằng cột đệm `Width="16"` giữa 2 panel (giống hệt cách tab Duplicator trong cùng file đã làm — không dùng divider line, chỉ dùng khoảng trắng). Panel phải đổi từ `Grid.Column="1"` → `"2"`, `Margin="16,12,16,0"` → `"0,12,16,0"` (bỏ margin trái 16 vì cột đệm đã lo phần đó). Verify: XML hợp lệ, `audit_tools.py --quiet` clean, `sync_wpf_styles.py --check` 53/53.
- **2026-07-03 — `ManaSched.xaml` (tab Excel Link): bảng DataGrid bên trái không chạm tới footer (bị "Selection bar" All/Clear/count + margin đáy 8px chặn phía dưới), trong khi panel phải (Preview) đã chạm tới footer sẵn — ĐÃ XỬ LÝ theo yêu cầu trực tiếp của user.** Đảo thứ tự hàng trong panel trái: chuyển "Selection bar" (All/Clear/count) lên TRƯỚC DataGrid (RowDefinitions Auto,Auto,Auto,* thay vì Auto,Auto,*,Auto), để DataGrid trở thành hàng cuối `Height="*"` — chạm sát footer. Đồng thời đổi `Margin` panel trái từ `"16,12,0,8"` → `"16,12,0,0"` (bỏ margin đáy 8px, khớp panel phải vốn đã `Margin="0,12,16,0"`). Panel phải không cần sửa (Preview ScrollViewer đã là hàng `*` áp chót trước ProgressBar collapsed=0 height, đã chạm footer từ trước). Verify: XML hợp lệ, không còn `x:Name` trùng lặp sau khi di chuyển (`xl_select_all_btn`/`xl_clear_btn`/`xl_count_text`/`xl_dg_schedules` mỗi cái đúng 1 lần), `audit_tools.py --quiet` clean, `sync_wpf_styles.py --check` 53/53.
- **2026-07-03 — `ManaSched.xaml` (tab Excel Link): footer action bar (Preview/Export/Import/Apply Changes) kéo dài full-width, lấn qua cả phần panel phải (Preview) vốn không liên quan tới thao tác Excel — ĐÃ XỬ LÝ (4 vòng lặp theo phản hồi user, chốt ở vòng 4).** Vòng 1 (user từ chối "KO"): thu hẹp cả thanh footer về đúng 300px + `WrapPanel` cho nút — user không muốn nút bị wrap. Vòng 2 (user từ chối, làm rõ "KO MUỐN FULL WIDTH"): thử full-width nền + nút lệch phải — sai vì nền vẫn full-width. Vòng 3 (đúng hướng): `Border` footer đổi hẳn sang `Grid.Column="2"` (chỉ tồn tại dưới cột phải/panel Preview) — nền + 4 nút + status đều nằm cột phải, cột trái không có footer.
  **Vòng 4 — bảng Selection vẫn chưa thực sự "chạm footer" (vì cột trái giờ trống hoàn toàn ở hàng footer) + thêm yêu cầu đồng bộ vị trí search/button giữa 2 cột**: tái cấu trúc hẳn Grid gốc của tab — bỏ Grid "content" trung gian (dư thừa, chỉ khai báo lại đúng 3 cột 300/16/* mà Grid gốc đã có), panel TRÁI (bảng Selection) giờ là con trực tiếp của Grid gốc với `Grid.RowSpan="2"` (ăn luôn vào hàng footer của Grid gốc — vì hàng đó ở cột 0 không còn gì, bảng tự do kéo dài xuống chạm đáy thật sự của tab, ngang bằng độ cao panel phải). Đồng thời gộp "Selection bar" (nút All/Clear/count) vào CÙNG hàng với tiêu đề "Select Schedules" (thành 1 header row: title bên trái + All/Clear/count bên phải, giống bố cục header-row+actions phổ biến), giảm panel trái từ 4 hàng (title/search/selection-bar/table) xuống còn 3 hàng (header-row/search/table) — khớp số hàng Auto với panel phải (Options/Info/Preview) để vị trí search pill và bảng thẳng hàng gần đúng với Options/Preview của panel phải. Verify: XML hợp lệ, không còn `x:Name` trùng lặp (`xl_select_all_btn`/`xl_clear_btn`/`xl_count_text`/`xl_search_box`/`xl_dg_schedules` mỗi cái đúng 1 lần), code-behind `ManaSchedDialog.py` wire đúng (không cần sửa vì tên giữ nguyên), `audit_tools.py --quiet` clean, `sync_wpf_styles.py --check` 53/53.
- Chưa phát sinh vấn đề khác trong Ngày 3.
