<Window xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Auto Work - T3Lab"
        Width="620" Height="700"
        MinWidth="560" MinHeight="580"
        WindowStartupLocation="CenterScreen"
        ResizeMode="CanResizeWithGrip"
        ShowInTaskbar="True"
        Background="White"
        FontFamily="Inter"
        FontSize="12">

    <WindowChrome.WindowChrome>
        <WindowChrome CaptionHeight="64"
                      ResizeBorderThickness="5"
                      GlassFrameThickness="0"
                      CornerRadius="8"
                      UseAeroCaptionButtons="False"/>
    </WindowChrome.WindowChrome>

    <Window.Resources>
        <Style x:Key="PrimaryButton" TargetType="Button">
            <Setter Property="Background" Value="#083D56"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Padding" Value="12,6"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="FontFamily" Value="Inter"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="3" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#062A3C"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Background" Value="#546E7A"/>
                                <Setter Property="Cursor" Value="Arrow"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SecondaryButton" TargetType="Button">
            <Setter Property="Background" Value="#F8F9FA"/>
            <Setter Property="Foreground" Value="#2C3E50"/>
            <Setter Property="Padding" Value="12,6"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="FontFamily" Value="Inter"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#546E7A"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="3" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#E2E6EA"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Foreground" Value="#546E7A"/>
                                <Setter Property="Cursor" Value="Arrow"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SuccessButton" TargetType="Button">
            <Setter Property="Background" Value="#27AE60"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Padding" Value="12,6"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="FontFamily" Value="Inter"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="3" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#1E8449"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Background" Value="#546E7A"/>
                                <Setter Property="Cursor" Value="Arrow"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="DangerButton" TargetType="Button">
            <Setter Property="Background" Value="#D32F2F"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Padding" Value="12,6"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="FontFamily" Value="Inter"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="3" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#B71C1C"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Background" Value="#546E7A"/>
                                <Setter Property="Cursor" Value="Arrow"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="WinCtrlButton" TargetType="Button">
            <Setter Property="Width" Value="40"/>
            <Setter Property="Height" Value="32"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="bd" Background="{TemplateBinding Background}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="bd" Property="Background" Value="#F8F9FA"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="CloseButton" TargetType="Button">
            <Setter Property="Width" Value="40"/>
            <Setter Property="Height" Value="32"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="bd" Background="{TemplateBinding Background}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="bd" Property="Background" Value="#D32F2F"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="NavTab" TargetType="RadioButton">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#7F8C8D"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="FontFamily" Value="Inter"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="4,6"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="RadioButton">
                        <Border x:Name="bd" BorderThickness="0,0,0,2" BorderBrush="Transparent" Padding="{TemplateBinding Padding}">
                            <ContentPresenter/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="bd" Property="BorderBrush" Value="#083D56"/>
                                <Setter Property="Foreground" Value="#083D56"/>
                                <Setter Property="FontWeight" Value="SemiBold"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Foreground" Value="#2C3E50"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="64"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- TITLE BAR -->
        <Grid Grid.Row="0" Background="White">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0" Orientation="Vertical" Margin="16,0,0,0" VerticalAlignment="Center"
                        WindowChrome.IsHitTestVisibleInChrome="True">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="T3Lab" FontSize="11" FontWeight="Bold" Foreground="#083D56" VerticalAlignment="Bottom" Margin="0,0,6,2"/>
                    <TextBlock Text="Auto Work" FontSize="18" FontWeight="Bold" Foreground="#2C3E50"/>
                </StackPanel>
                <Separator Height="1" Background="#546E7A" Margin="0,2,0,2"/>
                <TextBlock Text="Automate repetitive actions — quick click or record &amp; replay"
                           FontSize="10" Foreground="#7F8C8D" FontStyle="Italic"/>
            </StackPanel>

            <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Top"
                        WindowChrome.IsHitTestVisibleInChrome="True">
                <Button x:Name="btn_minimize" Style="{StaticResource WinCtrlButton}" Click="minimize_button_clicked" ToolTip="Minimize">
                    <TextBlock Text="&#xE921;" FontFamily="Segoe MDL2 Assets" FontSize="10" Foreground="#083D56"/>
                </Button>
                <Button x:Name="btn_maximize" Style="{StaticResource WinCtrlButton}" Click="maximize_button_clicked" ToolTip="Maximize">
                    <TextBlock Text="&#xE922;" FontFamily="Segoe MDL2 Assets" FontSize="10" Foreground="#083D56"/>
                </Button>
                <Button x:Name="btn_close" Style="{StaticResource CloseButton}" Click="close_button_clicked" ToolTip="Close">
                    <TextBlock Text="&#xE8BB;" FontFamily="Segoe MDL2 Assets" FontSize="10" Foreground="#083D56"/>
                </Button>
            </StackPanel>

            <Border Height="1" VerticalAlignment="Bottom" Background="#546E7A" Grid.ColumnSpan="2"/>
        </Grid>

        <!-- MODE SELECTOR -->
        <Border Grid.Row="1" Background="White" BorderBrush="#546E7A" BorderThickness="0,0,0,1" Padding="20,0">
            <StackPanel Orientation="Horizontal">
                <RadioButton x:Name="rb_quick" Style="{StaticResource NavTab}" GroupName="WorkMode"
                             IsChecked="True" Click="mode_changed" Margin="0,0,24,0">
                    <TextBlock Text="Quick Click"/>
                </RadioButton>
                <RadioButton x:Name="rb_record" Style="{StaticResource NavTab}" GroupName="WorkMode" Click="mode_changed">
                    <TextBlock Text="Record &amp; Replay"/>
                </RadioButton>
            </StackPanel>
        </Border>

        <!-- CONTENT TABS -->
        <TabControl x:Name="main_tabs" Grid.Row="2" BorderThickness="0" Background="White" Padding="0">
            <TabControl.ItemContainerStyle>
                <Style TargetType="TabItem">
                    <Setter Property="Visibility" Value="Collapsed"/>
                </Style>
            </TabControl.ItemContainerStyle>

            <!-- ── TAB 0: QUICK CLICK ── -->
            <TabItem>
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel Margin="24,16,24,16">

                        <!-- Click Location -->
                        <Border BorderBrush="#546E7A" BorderThickness="1" CornerRadius="3" Padding="16" Margin="0,0,0,12" Background="#F8F9FA">
                            <StackPanel>
                                <TextBlock Text="1. Click Location" FontSize="13" FontWeight="SemiBold" Foreground="#2C3E50" Margin="0,0,0,12"/>
                                <Grid Margin="0,0,0,10">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="110"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="110"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Grid.Column="0" Text="X Coordinate:" VerticalAlignment="Center" Foreground="#7F8C8D"/>
                                    <TextBox x:Name="txt_x" Grid.Column="1" Height="28" Text="500" VerticalContentAlignment="Center"
                                             Padding="8,2" BorderBrush="#546E7A" BorderThickness="1" Margin="0,0,12,0"/>
                                    <TextBlock Grid.Column="2" Text="Y Coordinate:" VerticalAlignment="Center" Foreground="#7F8C8D"/>
                                    <TextBox x:Name="txt_y" Grid.Column="3" Height="28" Text="500" VerticalContentAlignment="Center"
                                             Padding="8,2" BorderBrush="#546E7A" BorderThickness="1"/>
                                </Grid>
                                <Button Content="Pick Location on Screen  (3s countdown)" Style="{StaticResource PrimaryButton}"
                                        Click="pick_location_clicked" HorizontalAlignment="Left"/>
                            </StackPanel>
                        </Border>

                        <!-- Click Settings -->
                        <Border BorderBrush="#546E7A" BorderThickness="1" CornerRadius="3" Padding="16" Margin="0,0,0,12" Background="#F8F9FA">
                            <StackPanel>
                                <TextBlock Text="2. Click Settings" FontSize="13" FontWeight="SemiBold" Foreground="#2C3E50" Margin="0,0,0,12"/>
                                <Grid Margin="0,0,0,10">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="180"/>
                                        <ColumnDefinition Width="120"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Grid.Column="0" Text="Click interval (seconds):" VerticalAlignment="Center" Foreground="#7F8C8D"/>
                                    <TextBox x:Name="txt_interval" Grid.Column="1" Height="28" Text="1.0" VerticalContentAlignment="Center"
                                             Padding="8,2" BorderBrush="#546E7A" BorderThickness="1"/>
                                </Grid>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="180"/>
                                        <ColumnDefinition Width="120"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Grid.Column="0" Text="Total clicks:" VerticalAlignment="Center" Foreground="#7F8C8D"/>
                                    <TextBox x:Name="txt_clicks" Grid.Column="1" Height="28" Text="10" VerticalContentAlignment="Center"
                                             Padding="8,2" BorderBrush="#546E7A" BorderThickness="1"/>
                                </Grid>
                            </StackPanel>
                        </Border>

                        <!-- Tip -->
                        <Border BorderBrush="#083D56" BorderThickness="1" Background="#F8F9FA" CornerRadius="2" Padding="12" Margin="0,0,0,20">
                            <StackPanel>
                                <TextBlock Text="Tip:" FontWeight="Bold" Foreground="#062A3C" Margin="0,0,0,4" FontSize="11"/>
                                <TextBlock FontSize="11" Foreground="#2C3E50" TextWrapping="Wrap"
                                           Text="Click 'Pick Location', then point your mouse at the target — window minimizes 3 seconds while capturing. Press any key at any time to stop."/>
                            </StackPanel>
                        </Border>

                        <!-- Action buttons -->
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                            <Button Content="Close" Style="{StaticResource SecondaryButton}" Click="close_button_clicked" Width="80" Margin="0,0,12,0"/>
                            <Button Content="Start Auto Click" Style="{StaticResource SuccessButton}" Click="start_clicked" Width="148"/>
                        </StackPanel>
                    </StackPanel>
                </ScrollViewer>
            </TabItem>

            <!-- ── TAB 1: RECORD & REPLAY ── -->
            <TabItem>
                <Grid Margin="24,16,24,16">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <!-- Record controls -->
                    <Border Grid.Row="0" BorderBrush="#546E7A" BorderThickness="1" CornerRadius="3" Padding="16" Margin="0,0,0,12" Background="#F8F9FA">
                        <StackPanel>
                            <TextBlock Text="1. Record Actions" FontSize="13" FontWeight="SemiBold" Foreground="#2C3E50" Margin="0,0,0,12"/>
                            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                <Button x:Name="btn_start_record" Content="● Start Recording" Style="{StaticResource DangerButton}"
                                        Click="start_recording_clicked" Width="156"/>
                                <TextBlock x:Name="lbl_rec_hint" VerticalAlignment="Center" Margin="14,0,0,0"
                                           FontSize="11" Foreground="#7F8C8D"
                                           Text="Press any keyboard key to stop recording"/>
                            </StackPanel>
                        </StackPanel>
                    </Border>

                    <!-- Actions list -->
                    <Border Grid.Row="1" BorderBrush="#546E7A" BorderThickness="1" CornerRadius="3" Padding="16" Margin="0,0,0,12" Background="White">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>
                            <Grid Grid.Row="0" Margin="0,0,0,8">
                                <TextBlock Text="2. Recorded Actions" FontSize="13" FontWeight="SemiBold" Foreground="#2C3E50"/>
                                <TextBlock x:Name="lbl_action_count" Text="0 actions" HorizontalAlignment="Right"
                                           Foreground="#7F8C8D" FontSize="11" VerticalAlignment="Center"/>
                            </Grid>
                            <ListBox x:Name="lst_actions" Grid.Row="1"
                                     BorderBrush="#546E7A" BorderThickness="1"
                                     FontFamily="Consolas" FontSize="11"
                                     Background="White" Foreground="#2C3E50"
                                     ScrollViewer.HorizontalScrollBarVisibility="Auto"/>
                        </Grid>
                    </Border>

                    <!-- Replay controls -->
                    <Border Grid.Row="2" BorderBrush="#546E7A" BorderThickness="1" CornerRadius="3" Padding="16" Margin="0,0,0,12" Background="#F8F9FA">
                        <StackPanel>
                            <TextBlock Text="3. Replay" FontSize="13" FontWeight="SemiBold" Foreground="#2C3E50" Margin="0,0,0,12"/>
                            <Grid Margin="0,0,0,12">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="180"/>
                                    <ColumnDefinition Width="80"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Grid.Column="0" Text="Repeat (loops):" VerticalAlignment="Center" Foreground="#7F8C8D"/>
                                <TextBox x:Name="txt_loops" Grid.Column="1" Height="28" Text="1"
                                         VerticalContentAlignment="Center" Padding="8,2"
                                         BorderBrush="#546E7A" BorderThickness="1"/>
                            </Grid>
                            <StackPanel Orientation="Horizontal">
                                <Button x:Name="btn_play" Content="▶  Play" Style="{StaticResource SuccessButton}"
                                        Click="play_clicked" Width="110" IsEnabled="False"/>
                                <Button x:Name="btn_clear" Content="Clear" Style="{StaticResource SecondaryButton}"
                                        Click="clear_clicked" Width="80" Margin="12,0,0,0"/>
                            </StackPanel>
                        </StackPanel>
                    </Border>

                    <!-- Tip -->
                    <Border Grid.Row="3" BorderBrush="#083D56" BorderThickness="1" Background="#F8F9FA" CornerRadius="2" Padding="12">
                        <StackPanel>
                            <TextBlock Text="Tip:" FontWeight="Bold" Foreground="#062A3C" Margin="0,0,0,4" FontSize="11"/>
                            <TextBlock FontSize="11" Foreground="#2C3E50" TextWrapping="Wrap"
                                       Text="Click 'Start Recording', perform your mouse actions (left/right click), then press any keyboard key to stop. Click 'Play' to replay the exact sequence with original timing."/>
                        </StackPanel>
                    </Border>
                </Grid>
            </TabItem>
        </TabControl>

        <!-- STATUS BAR -->
        <Border Grid.Row="3" Background="#F8F9FA" BorderBrush="#546E7A" BorderThickness="0,1,0,0" Padding="14,8">
            <TextBlock x:Name="status_text" FontSize="11" Foreground="#7F8C8D" Text="Ready"/>
        </Border>

        <!-- Copyright added automatically -->
        <TextBlock Text="© Copyright by T3Lab" HorizontalAlignment="Right" VerticalAlignment="Bottom" Margin="0,0,14,8" Foreground="#3498DB" FontSize="11" IsHitTestVisible="False" Panel.ZIndex="999"/>
    </Grid>
</Window>
