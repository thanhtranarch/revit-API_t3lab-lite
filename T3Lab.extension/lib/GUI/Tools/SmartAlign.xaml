<Window xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Smart Align"
        Width="320" Height="250"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Background="#E4E4E7"
        Topmost="True"
        FontFamily="Hanken Grotesk">

    <WindowChrome.WindowChrome>
        <WindowChrome CaptionHeight="64"
                      ResizeBorderThickness="5"
                      GlassFrameThickness="0"
                      CornerRadius="22"
                      UseAeroCaptionButtons="False"/>
    </WindowChrome.WindowChrome>

    <Window.Resources>
        <!-- ═══ Central Color Brushes ═══ -->
        <SolidColorBrush x:Key="T3PrimaryInkBrush" Color="#18181B"/>
        <SolidColorBrush x:Key="T3PrimaryTextBrush" Color="#27272A"/>
        <SolidColorBrush x:Key="T3SecondaryTextBrush" Color="#3F3F46"/>
        <SolidColorBrush x:Key="T3TertiaryTextBrush" Color="#71717A"/>
        <SolidColorBrush x:Key="T3MainAppBgBrush" Color="#E4E4E7"/>
        <SolidColorBrush x:Key="T3ComponentBgBrush" Color="#FFFFFF"/>

        <!-- PRIMARY BUTTON - solid deep slate, white text -->
        <Style x:Key="PrimaryButton" TargetType="Button">
            <Setter Property="Background"      Value="#18181B"/>
            <Setter Property="Foreground"      Value="White"/>
            <Setter Property="Padding"         Value="4"/>
            <Setter Property="FontSize"        Value="11"/>
            <Setter Property="FontFamily"      Value="Hanken Grotesk"/>
            <Setter Property="FontWeight"      Value="SemiBold"/>
            <Setter Property="Height"          Value="34"/>
            <Setter Property="Cursor"          Value="Hand"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="8"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#000000"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="#18181B"/>
                </Trigger>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Background" Value="#F4F4F6"/>
                    <Setter Property="Foreground" Value="#9A9AA2"/>
                    <Setter Property="Cursor"     Value="Arrow"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- WINDOW CONTROL BUTTON -->
        <Style x:Key="WinCtrlButton" TargetType="Button">
            <Setter Property="Width"           Value="40"/>
            <Setter Property="Height"          Value="32"/>
            <Setter Property="Background"      Value="Transparent"/>
            <Setter Property="Foreground"      Value="#18181B"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor"          Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="bd" Background="{TemplateBinding Background}" CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#E4E4E7"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- CLOSE BUTTON -->
        <Style x:Key="CloseButton" TargetType="Button" BasedOn="{StaticResource WinCtrlButton}">
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#D23B3B"/>
                    <Setter Property="Foreground" Value="White"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Border BorderBrush="#A1A1AA" BorderThickness="1.5" CornerRadius="22" ClipToBounds="True" Background="#E4E4E7">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="64"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="24"/>
            </Grid.RowDefinitions>

            <!-- TITLE BAR -->
            <Grid Grid.Row="0" Background="White" Height="64">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- Brand + title -->
                <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center"
                            Margin="16,0,0,0" WindowChrome.IsHitTestVisibleInChrome="True">
                    <StackPanel Orientation="Vertical" VerticalAlignment="Center">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="T3Lab" FontSize="11" FontWeight="Bold"
                                       Foreground="#0F172A" VerticalAlignment="Bottom" Margin="0,0,8,2"/>
                            <TextBlock Text="Smart Align" FontSize="18" FontWeight="Bold" Foreground="#0F172A"/>
                        </StackPanel>
                        <Separator Height="1" Background="#E2E8F0" Margin="0,2,0,2"/>
                        <TextBlock Text="Align and distribute selected Revit elements" FontSize="9" Foreground="#64748B" FontStyle="Italic"/>
                    </StackPanel>
                </StackPanel>

                <!-- Window control buttons -->
                <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center"
                            Margin="0,0,5,0" WindowChrome.IsHitTestVisibleInChrome="True">
                    <Button x:Name="btn_minimize" Style="{StaticResource WinCtrlButton}" ToolTip="Minimize">
                        <TextBlock Text="&#xE921;" FontFamily="Segoe MDL2 Assets" FontSize="10"/>
                    </Button>
                    <Button x:Name="btn_close_chrome" Style="{StaticResource CloseButton}" ToolTip="Close">
                        <TextBlock Text="&#xE8BB;" FontFamily="Segoe MDL2 Assets" FontSize="10"/>
                    </Button>
                </StackPanel>

                <!-- Title bar bottom border -->
                <Border Grid.ColumnSpan="3" Height="1" VerticalAlignment="Bottom" Background="#E2E8F0"/>
            </Grid>

            <!-- CONTENT: Grid of Alignment Options -->
            <Grid Grid.Row="1" Margin="12,10,12,10">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <TextBlock Text="Select elements in Revit first, then click an alignment below:"
                           FontSize="10" Foreground="#64748B" Margin="4,0,4,8" TextWrapping="Wrap"/>

                <Grid Grid.Row="1">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <!-- Row 1: Horizontal Alignments -->
                    <Button x:Name="btn_align_left" Grid.Row="0" Grid.Column="0" Margin="3" Style="{StaticResource PrimaryButton}" ToolTip="Align Left">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="&#xF138;" FontFamily="Segoe MDL2 Assets" FontSize="14" Margin="0,0,4,0"/>
                            <TextBlock Text="Left"/>
                        </StackPanel>
                    </Button>
                    <Button x:Name="btn_align_center_h" Grid.Row="0" Grid.Column="1" Margin="3" Style="{StaticResource PrimaryButton}" ToolTip="Align Horizontal Center">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="&#xF13A;" FontFamily="Segoe MDL2 Assets" FontSize="14" Margin="0,0,4,0"/>
                            <TextBlock Text="Center H"/>
                        </StackPanel>
                    </Button>
                    <Button x:Name="btn_align_right" Grid.Row="0" Grid.Column="2" Margin="3" Style="{StaticResource PrimaryButton}" ToolTip="Align Right">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="&#xF139;" FontFamily="Segoe MDL2 Assets" FontSize="14" Margin="0,0,4,0"/>
                            <TextBlock Text="Right"/>
                        </StackPanel>
                    </Button>

                    <!-- Row 2: Vertical Alignments -->
                    <Button x:Name="btn_align_top" Grid.Row="1" Grid.Column="0" Margin="3" Style="{StaticResource PrimaryButton}" ToolTip="Align Top">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="&#xF137;" FontFamily="Segoe MDL2 Assets" FontSize="14" Margin="0,0,4,0"/>
                            <TextBlock Text="Top"/>
                        </StackPanel>
                    </Button>
                    <Button x:Name="btn_align_center_v" Grid.Row="1" Grid.Column="1" Margin="3" Style="{StaticResource PrimaryButton}" ToolTip="Align Vertical Center">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="&#xF13B;" FontFamily="Segoe MDL2 Assets" FontSize="14" Margin="0,0,4,0"/>
                            <TextBlock Text="Center V"/>
                        </StackPanel>
                    </Button>
                    <Button x:Name="btn_align_bottom" Grid.Row="1" Grid.Column="2" Margin="3" Style="{StaticResource PrimaryButton}" ToolTip="Align Bottom">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="&#xF136;" FontFamily="Segoe MDL2 Assets" FontSize="14" Margin="0,0,4,0"/>
                            <TextBlock Text="Bottom"/>
                        </StackPanel>
                    </Button>

                    <!-- Row 3: Distributions -->
                    <Button x:Name="btn_distribute_h" Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="2" Margin="3" Style="{StaticResource PrimaryButton}" ToolTip="Distribute Horizontally">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="&#xF13C;" FontFamily="Segoe MDL2 Assets" FontSize="14" Margin="0,0,6,0"/>
                            <TextBlock Text="Distribute Horizontally"/>
                        </StackPanel>
                    </Button>
                    <Button x:Name="btn_distribute_v" Grid.Row="2" Grid.Column="2" Margin="3" Style="{StaticResource PrimaryButton}" ToolTip="Distribute Vertically">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="&#xF13D;" FontFamily="Segoe MDL2 Assets" FontSize="14" Margin="0,0,4,0"/>
                            <TextBlock Text="Distribute V"/>
                        </StackPanel>
                    </Button>
                </Grid>
            </Grid>

            <!-- STATUS BAR -->
            <Border Grid.Row="2" Background="#F8FAFC" BorderBrush="#E2E8F0" BorderThickness="0,1,0,0" Padding="14,4">
                <TextBlock x:Name="status_text" Text="Select elements in Revit first"
                           FontSize="9" Foreground="#64748B" FontStyle="Italic"/>
            </Border>
        </Grid>
    </Border>
</Window>
